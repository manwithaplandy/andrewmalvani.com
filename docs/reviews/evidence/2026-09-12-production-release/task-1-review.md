# Task 1 independent review

## Spec compliance

**Issues found — the final API behavior is implemented, but the current live migration plan is unsafe (T1-I01).** Reviewed `04facf51651a1733a38a6f25a8e466c9cf4deee4` → `cf8410c12d42179222371e26590dcb531d208f5e` using the supplied task package, brief, plan and evidence. The earlier whole-branch approval is outside this review.

The health mapping, exact two-origin CORS, preserved contact contract, meaningful deployment fingerprint and sole explicit stage owner satisfy their source requirements. The source does not establish a safe replacement of the existing OPTIONS integration. The controller's subsequently supplied real plan exposes that gap.

**Cannot verify as completed:** production environment preparation, checked-code installation, the proposed one-time integration migration, its refreshed plan, live health/CORS/error behavior, actual SNS delivery, or analytics release gates. None was performed by this reviewer. Local mocks and archive tests do not establish those results.

## Strengths

- The health MOCK now supplies the missing status mapping at `/Users/andrew/Scripts/react-resume/.worktrees/design-ux-remediation/terraform/contactLambda.tf:265`. `/Users/andrew/Scripts/react-resume/.worktrees/design-ux-remediation/docs/operations/delivery.md:17` correctly limits health success to API reachability, rather than notification delivery.
- Origin selection is centralized in the existing handler and authoritative Terraform configuration. `/Users/andrew/Scripts/react-resume/.worktrees/design-ux-remediation/sns_publish_lambda/lambda_function.py:37` returns an origin grant only for an exact allowed origin and includes `Vary: Origin`; line 87 rejects unknown origins and handles OPTIONS before parsing or publishing. Missing-Origin clients remain compatible. No new service, authentication claim or contact-data logging was added.
- `/Users/andrew/Scripts/react-resume/.worktrees/design-ux-remediation/tests/contact/test_handler.py:60` exercises both public origins across success and clean failures; lines 78, 87, 97 and 117 cover no-publish preflight, lookalikes, missing Origin, limits and sanitization. Line 141 runs the same contract against the actual checked ZIP, with isolated fake SDK clients. The source and archive boundary is meaningfully tested.
- `/Users/andrew/Scripts/react-resume/.worktrees/design-ux-remediation/terraform/contactLambda.tf:85` fingerprints substantive method/integration/response configuration and waits for the responses and invocation permission. Line 125 retains the explicit stage. `/Users/andrew/Scripts/react-resume/.worktrees/design-ux-remediation/tests/infra/test_contact_api.py:77` verifies stable fingerprints and actual configuration mutations, rather than formatting changes. These fresh-state tests correctly remain separate from live-transition proof.

## Issues

### Critical

None.

### Important — T1-I01: replacing OPTIONS can delete the newly installed integration

**Evidence:** `/Users/andrew/Scripts/react-resume/.worktrees/design-ux-remediation/terraform/contactLambda.tf:216` changes the existing OPTIONS integration from MOCK to AWS_PROXY/POST. The deployment's lifecycle at line 120 propagates create-before-destroy to its dependencies. The controller's real plan, summarized at `/private/tmp/react-resume-release-20260911/candidate-plan-summary.json:16`, orders this integration as **create, then delete**. Selected private-plan fields confirm the old and new integration share exactly the same REST API, resource and OPTIONS method; `type` and `integration_http_method` cause the replacement.

The locked AWS provider 5.50.0 marks those two arguments ForceNew, creates with `PutIntegration` and deletes with `DeleteIntegration`. Both operations use only that same three-part identity; deletion does not distinguish an old integration version from the new one. See the provider's [integration implementation](https://github.com/hashicorp/terraform-provider-aws/blob/v5.50.0/internal/service/apigateway/integration.go#L161), especially creation lines 165–169/226–232 and deletion lines 470–479, and AWS's [PutIntegration](https://docs.aws.amazon.com/apigateway/latest/api/API_PutIntegration.html) and [DeleteIntegration](https://docs.aws.amazon.com/apigateway/latest/api/API_DeleteIntegration.html) contracts. Terraform documents [lifecycle propagation](https://developer.hashicorp.com/terraform/language/meta-arguments/lifecycle#create_before_destroy-and-resource-dependencies).

**Impact and severity:** if creation succeeds, later cleanup targets the newly installed singleton integration. An already captured deployment might continue serving, but the configured API would be left without the intended OPTIONS integration, undermining subsequent deployment and recovery. This is an Important release-correctness defect, not evidence that a live outage has occurred. Merely preserving the stage's identity does not resolve it.

**Required outcome:** do not apply that replacement plan. The controller's proposed one-time in-place `PutIntegration`, followed by refresh and a new reviewed plan, is an acceptable remedy without reverting the design to MOCK or removing/importing state. Guard the exact existing tuple and expected old configuration, or verify an already exact new configuration. Keep the current stage deployment unchanged during preparation. Require the refreshed plan to contain no OPTIONS deletion/replacement, preserve API/stage/Lambda identities, and capture the final response configuration in the new deployment. Preserve the exact environment-add → checked-code → integration-preparation → refreshed-plan → deployment sequence and abort conditions in `/Users/andrew/Scripts/react-resume/.worktrees/design-ux-remediation/docs/operations/delivery.md:47`. Retain focused rejection of the unsafe old plan and acceptance evidence for the prepared plan; fresh-state mocks cannot substitute for that boundary. This remedy was proposed and assessed, not performed or verified here.

### Minor — T1-M01: native mock runs contain expected targeting warnings

`/Users/andrew/Scripts/react-resume/.worktrees/design-ux-remediation/tests/infra/contact-api.tftest.hcl:33` deliberately targets the contact stage. The final native logs contain “Resource targeting is in effect” and “Applied changes may be incomplete”; for example, `/Users/andrew/Scripts/react-resume/.worktrees/design-ux-remediation/.superpowers/sdd/2026-09-11-api-analytics-release/evidence/infra-final/configuration.log:7` and line 8.

These diagnostics do not invalidate the bounded mock contract, and no live targeted apply occurred. Keep them explicitly classified as expected test-scope warnings, distinguish unexpected diagnostics, and do not describe these outputs as warning-free or as complete infrastructure validation. This is non-blocking evidence hygiene; it does not warrant an unrelated full suite rerun.

## Focused checks and evidence

- **Legacy environment transition:** a credential-isolated fake-SNS invocation of both source and checked ZIP with only the existing singular `ALLOWED_ORIGIN` reproduced an empty allowed-origin set, HTTP 403 for a valid apex request, no origin grant, no SNS calls and no logged bytes. `/Users/andrew/Scripts/react-resume/.worktrees/design-ux-remediation/sns_publish_lambda/lambda_function.py:14` explains that result. The controller's explicit ruling resolves this transition procedurally: privately preserve every existing environment key, including the singular origin and SNS target, add the exact plural JSON list, await and verify successful configuration, then install the checked ZIP. Old code ignores the extra key; new code has its required configuration before installation. Terraform may then remove the old singular key. A code-only-first rollout remains invalid; no fallback production code is required if this sequence is enforced.
- **Replacement and stage ownership:** inspected only selected safe fields from the controller's private real plan and the exact provider 5.50.0 integration/deployment implementations. The API is no-op, explicit stage is an update, and Lambdas are not replaced. The provider's [deployment cleanup](https://github.com/hashicorp/terraform-provider-aws/blob/v5.50.0/internal/service/apigateway/deployment.go#L173) checks whether a stage still points at the retiring deployment before deleting it; updating the explicit stage first addresses that separate concern. It does not protect the OPTIONS singleton.
- **Existing results inspected, not rerun:** final logs show 14 contact source/archive tests, 2 infrastructure tests containing 7 native mock evaluations, 4 packaging tests and 12 release/pipeline tests passing. Readonly backend-free init and Terraform validate passed; formatting, actionlint and diff-check logs are empty. Native mock initialization used an offline mirror and labels packages unauthenticated; this is not a claim of fresh signature verification. The recorded lock checksum and provider versions are unchanged. The retained red runs show the intended missing-header and ineffective-fingerprint failures.
- **Artifact identity:** the preserved manifest verifies contact ZIP SHA-256 `4031b6f438e55a6547db3ea390b4292328652e67212a78865d617ffe0a515902` and unchanged stats ZIP SHA-256 `b68c47a7774b1d575b910a60d7ceded54cf9d2b9a398334f00476113cbc945ca`. No frontend, analytics producer, package generator or main deployment workflow change appears in this task's package. No broad suite, cloud query, production invocation, git mutation or implementation edit was performed by this reviewer.

## Assessment

**Task quality: Needs fixes.** The handler and final configuration are small, coherent and meaningfully tested, but T1-I01 prevents approval of the current migration. The accepted preparation procedure must be made durable and the unsafe replacement excluded before the task can pass its release-safety gate. Local source/test success and the earlier whole-branch approval do not approve production operations; all existing artifact, analytics and live-verification gates remain binding.
